// Minimal Metro config; customize if you add monorepo or asset plugins
// Expo provides sane defaults; this file is optional.
module.exports = {
  resolver: {
    // Add any custom sourceExts or resolver options here if needed
  },
};
